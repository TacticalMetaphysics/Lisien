# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py312,py313,py314

[testenv]
passenv = TMPDIR, PYTEST_KEYWORDS
recreate = true
change_dir = .
deps =
	pytest
commands =
	pytest -vv --pyargs lisien.tests -k "{env:PYTEST_KEYWORDS}"
