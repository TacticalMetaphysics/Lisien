# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py312,py313,py314

[testenv]
passenv = TMPDIR
recreate = true
change_dir = {env_tmp_dir}
deps =
	pytest
commands =
	pytest -vvx -k 'not slow and not big and not subinterpreter and sqlite' --pyargs lisien.tests
