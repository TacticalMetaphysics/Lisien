# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py310,py311,py312,py313

[testenv]
# install pytest in the virtualenv where commands will be executed
passenv = DISPLAY
recreate = true
deps =
	pytest
    ../lisien/
commands =
    pytest -x --pyargs elide
